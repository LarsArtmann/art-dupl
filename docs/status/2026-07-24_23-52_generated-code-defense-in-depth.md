# Status: Generated Code Defense-in-Depth (Content-Based _templ.go Exclusion)

**Date:** 2026-07-24 23:52
**Branch:** `fork` (21 commits ahead of origin)
**Task:** Add content-based header check for `// Code generated by templ - DO NOT EDIT` when `_templ.go` files bypass both gogenfilter's `FilterTempl` and `.gitignore` honoring.

---

## a) FULLY DONE

### Implementation

- **`filterExcludedGenerated()` function** (`cmd/util.go`): Content-based defense-in-depth check. When `--include-generated generic` (or `all`) disables `FilterGeneric`, the filename-gated category filters (`FilterTempl`/`FilterSQLC`/`FilterProtobuf`) only match files with expected suffixes (`_templ.go`, `_sqlc.go`, `*.pb.go`). This function catches generated files that lack the expected suffix by checking content markers directly.
- **`generatorIncludes.Generic` field**: New field tracks whether `--include-generated generic` or `all` is active. Without this, the content-reading path in `shouldIncludeFile` would not activate when only `Generic` is set.
- **Wired into `shouldIncludeFile`**: Runs after gogenfilter's verdict, only when file passed and content is already loaded. No extra I/O. Stats recorded with correct `FilterReason`.
- **Respects explicit includes**: If user runs `--include-generated templ`, the templ marker check is skipped (`!includes.Templ` is false). Correct override behavior.

### Tests

- **`TestFilterExcludedGenerated`** (8 sub-tests): Unit tests for all marker categories (templ, sqlc, protobuf) — included vs. not-included, regular content, empty content.
- **`TestShouldIncludeFile_TemplContentDefenseInDepth`** (5 sub-tests): Integration tests with real files on disk. Tests the actual gap scenario: templ content in a file WITHOUT `_templ.go` suffix, with `FilterGeneric` disabled. Also tests suffix files and regular files.
- **`newTestFilter` helper**: Extracted to reduce filter construction duplication in tests.
- **Updated existing tests**: `TestGeneratorIncludesAny` and `TestGeneratorIncludesAllowsContent` updated for new `Generic` field.

### Documentation

- **AGENTS.md**: Updated the "Generated code filtered by default" bullet to document the content-based defense-in-depth mechanism, the `Generic` field, and `filterExcludedGenerated`. Fixed stale reference `allowsFile` → `allowsContent`.

### Quality Gates

- `go build ./...` — clean
- `go test ./...` — 26/26 packages pass
- `golangci-lint run --timeout 5m ./...` — 0 issues
- `nix flake check` — 9/9 checks pass

---

## b) PARTIALLY DONE

### CHANGELOG.md

- NOT updated with this fix. The `[Unreleased]` section from the previous session exists but does not mention the defense-in-depth content check.

### Commit hygiene

- Auto-committer committed `cmd/util.go` and `cmd/filter_includes_test.go` in 2 commits (`0200cc90`, `13d0e4d0`). `AGENTS.md` remains uncommitted (modified, not staged).
- Commit messages are **inaccurate**: describe "filter inclusion logic", "artwork files", "filter parsing and validation" — none of which match the actual content (templ/sqlc/protobuf content-based defense-in-depth).

---

## c) NOT STARTED

- **Race detector on new tests**: Did not run `go test -race` on the `cmd` package specifically for the new tests. Tests are non-concurrent but the race detector is part of the CI standard.
- **BDD/integration test**: No end-to-end test that runs the actual CLI binary with `--include-generated generic` and verifies a non-suffix templ file is excluded from clone detection output.
- **HOW_TO_USE.md**: Not updated. Likely unnecessary since this is an internal implementation detail, not a user-facing behavior change.
- **Dead `//nolint:exhaustruct` directives**: 10 directives across 7 files now reference a disabled linter. Pre-existing issue, not touched.

---

## d) TOTALLY FUCKED UP

### Auto-committer commit messages (recurring problem)

Commits `0200cc90` and `13d0e4d0` have **fabricated, inaccurate commit messages**:

- Claims "tests covering various filter scenarios (include/exclude patterns)" — actually tests content-based generated code defense-in-depth
- Claims "artwork files" — this is a Go code clone detection tool, not an artwork project
- Claims "filter parsing and validation" — no parsing or validation was added
- Claims "helper functions for filter parsing" — the helper is `filterExcludedGenerated`, a content marker checker

This is the **same problem flagged in the previous session's status report** (`docs/status/2026-07-24_23-34_post-sprint-cleanup-and-hardening.md`). The auto-committer is actively corrupting git history with hallucinated descriptions.

### AGENTS.md left uncommitted

The documentation update is sitting in the working tree, uncommitted. The code it documents is already committed. This means the committed code has no documentation in the same history.

---

## e) WHAT WE SHOULD IMPROVE

### Architectural concerns

1. **`generatorIncludes` struct is growing unbounded.** It now has 6 boolean fields (SQLC, Templ, Protobuf, Mockgen, Stringer, Generic). Adding a new generated-code category requires touching the struct, `any()`, `allowsContent()`, `filterExcludedGenerated()`, `newGeneratorIncludes()`, and the config. This is a shotgun surgery pattern. Consider a `map[string]struct{}` or a bitfield.

2. **`filterExcludedGenerated` duplicates marker constants.** It reuses `templMarker`, `sqlcMarker`, `protobufMarker` from `cmd/util.go`, but the function's logic is parallel to `allowsContent` — both switch on the same markers with opposite polarity. These could be unified into a single "categorize content" function that returns the category, then callers decide include/exclude.

3. **Two code paths for filtering.** `shouldIncludeFile` (with includes) reads content upfront; `shouldIncludeFileStandard` (without includes) does two-phase without upfront read. The defense-in-depth check only runs on the includes path. This is correct (the standard path always has `FilterGeneric`), but the asymmetry is fragile — if someone ever disables `FilterGeneric` without setting any includes flag, the gap reopens with no content check.

4. **`shouldIncludeFile` is becoming complex.** It now has 4 distinct branches: nil filter, no-includes standard path, generic-override path, defense-in-depth path. Gocyclo is under threshold but the function is doing a lot. Consider extracting the defense-in-depth check into the gogenfilter library itself (it already has `FilterDetailedWithContent`).

5. **No content-based detection for mockgen/stringer gap.** `filterExcludedGenerated` only checks templ/sqlc/protobuf. Mockgen and stringer are content-only in gogenfilter (not filename-gated), so their filters catch them regardless of suffix. But if gogenfilter ever changes this, the gap would reopen silently.

### Process concerns

6. **Auto-committer should be disabled or fixed.** It has produced inaccurate commit messages in TWO consecutive sessions. The messages are not just generic — they are actively wrong, describing work that was not done. This is worse than no commit message.

7. **No integration test for the actual CLI.** All tests are at the function level. An end-to-end test that creates a directory with a non-suffix templ file, runs `art-dupl --include-generated generic`, and checks the output would catch regressions in the full pipeline.

8. **Test table style inconsistency.** The `TestFilterExcludedGenerated` test table uses named fields (`name:`, `content:`, etc.) because gofumpt requires it for multi-field structs. But older test tables in the same file use positional fields. The codebase has mixed style.

---

## f) Up to 50 Things We Should Get Done Next

### High priority (correctness & hygiene)

1. **Commit `AGENTS.md`** — it's sitting uncommitted in the working tree
2. **Fix or disable the auto-committer** — it's producing fabricated commit messages
3. **Update `CHANGELOG.md`** with the defense-in-depth content check entry
4. **Run `go test -race ./cmd/...`** to verify the new tests under the race detector
5. **Fix the 10 dead `//nolint:exhaustruct` directives** across 7 files (now that exhaustruct is disabled)
6. **Fix `bdd/type_aware_test.go:14` — `undefined: CreateBDDTestSetup`** — LSP typecheck error, pre-existing
7. **Fix LSP diagnostics in `cmd/progress_test.go`** — 7 warnings (errcheck, wsl_v5), pre-existing from previous session
8. **Fix LSP diagnostic in `cmd/accept_directive.go:97`** — mnd magic number 64, pre-existing
9. **Fix LSP diagnostic in `cmd/accept_directive_test.go:40`** — gci formatting, pre-existing

### Medium priority (robustness)

10. **Add CLI integration test** for `--include-generated generic` with non-suffix templ file
11. **Unify `allowsContent` and `filterExcludedGenerated`** into a single content categorization function
12. **Consider refactoring `generatorIncludes`** from 6 booleans to a set/map pattern
13. **Add content-based detection for mockgen/stringer** in `filterExcludedGenerated` for future-proofing
14. **Extract defense-in-depth logic into gogenfilter library** itself (upstream contribution)
15. **Annotate old status report** `docs/status/2026-07-24_23-11_full-todo-execution-sprint.md` as SUPERSEDED (describes removed stub flags as "partially done")
16. **Resolve SDK DefaultOptions threshold split-brain** (SDK=15 vs CLI=5)
17. **Fix SDK TypeAware fallback test** that asserts nothing (`_ = result; _ = err`)
18. **Fix progress test parallelism issue** (`os.Stderr` manipulation without parallel guard)

### Low priority (polish)

19. **Document the filtering architecture** in a dedicated `docs/FILTERING.md` or ADR
20. **Add `--list-filters` flag** to show active filter configuration
21. **Consider `.artduplignore`** as an alternative to `.gitignore` for art-dupl-specific exclusions
22. **Add filter statistics to JSON output** (currently only in text/stats mode)
23. **Profile the content-reading path** — every included file gets `os.ReadFile` even if gogenfilter would have caught it by filename alone
24. **Consider lazy content reading** — only read content if the filename-based filter didn't match
25. **Add `--debug-filters` flag** to trace why each file was included/excluded
26. **Standardize test table style** — all named fields or all positional, not mixed
27. **Add property-based test** for filter consistency (file is filtered iff it should be)
28. **Consider caching file content** across filter checks to avoid re-reading
29. **Document `filterExcludedGenerated` in `docs/ACTIONABILITY_PATTERNS.md`** or equivalent
30. **Add `FilterReason` to the stats output** for visibility into defense-in-depth catches
31. **Consider extending content markers** for other generators (wire, ent, gqlgen, etc.)
32. **Add test for `--include-generated all`** specifically (all categories included, nothing filtered)
33. **Add test for mixed includes** (e.g., `--include-generated generic,templ` — generic on, templ on)
34. **Review whether `Generic` field should be in `generatorIncludes` at all** — it's not a "category to include" like the others, it's a meta-flag
35. **Consider renaming `filterExcludedGenerated`** to `detectBypassedGenerated` or similar — "excluded" is ambiguous
36. **Add benchmark** for `filterExcludedGenerated` on large files
37. **Consider early-exit optimization** — if file doesn't contain "Code generated", skip all marker checks
38. **Add test for binary files** — `strings.Contains` on binary content could have false positives
39. **Consider using `bytes.Contains`** instead of `string(content)` conversion for performance
40. **Review error handling in content read path** — currently returns `true` (include) on read error, which is fail-open

### Documentation

41. **Update `HOW_TO_USE.md`** with `--include-generated` behavior details
42. **Add ADR for the defense-in-depth filtering architecture**
43. **Update `FEATURES.md`** if generated-code filtering isn't already documented there
44. **Review all status reports in `docs/status/`** for staleness
45. **Consolidate filtering documentation** — currently spread across AGENTS.md, HOW_TO_USE.md, and inline comments

### Technical debt

46. **Pay down the `shouldIncludeFile` complexity** — extract filter override logic
47. **Consider a `FileFilter` interface** that composes gogenfilter + content checks
48. **Review the `FilterStats` API** — `Record` takes `FilterResult` but defense-in-depth constructs a synthetic one
49. **Add `FilterResult.Source` field** to distinguish gogenfilter vs defense-in-depth catches in stats
50. **Consider whether `FilterGeneric` should ever be disabled** — maybe `--include-generated generic` should mean "include ALL generated code" and set all category flags

---

## g) Questions (cannot figure out myself)

### 1. Should the auto-committer be disabled entirely?

It has produced inaccurate commit messages in two consecutive sessions. The messages describe work that wasn't done ("artwork files", "filter parsing") and miss the actual changes (defense-in-depth content check). The alternative is manual commits, which risks leaving work uncommitted. I cannot determine whether the auto-committer is a Crush hook, a git hook, or an external tool, nor whether its configuration can be improved.

### 2. Is the defense-in-depth check the right architectural location, or should this be pushed upstream into gogenfilter?

The gap exists because gogenfilter's `FilterTempl`/`FilterSQLC`/`FilterProtobuf` are filename-gated (require `_templ.go`/`_sqlc.go`/`*.pb.go` suffix) while `FilterGeneric` (content-based "Code generated by") is the real catch-all. When `FilterGeneric` is disabled, the filename-gated filters are insufficient. The "right" fix is in gogenfilter: make the category filters content-based even without the suffix, OR make `FilterDetailed` always run a content-based generic check as a fallback. But that changes the library's contract. Should I open an issue/PR against gogenfilter, or keep the workaround in art-dupl?

### 3. Should `--include-generated generic` even exist as a separate option?

Currently `generic` means "disable the generic catch-all filter." But the whole point of the generic filter is to catch generated files that don't match any specific category. Disabling it without enabling all specific categories creates the gap that `filterExcludedGenerated` now patches. An alternative design: `--include-generated generic` would implicitly enable ALL categories (sqlc, templ, protobuf, mockgen, stringer), making it equivalent to `all`. Or: remove `generic` as a user-facing option entirely and only allow specific categories + `all`. I cannot determine the original design intent.
