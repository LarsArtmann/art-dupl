# CI Fix Status Report — 2026-07-22 11:21

**Session goal:** Fix all failing CI/CD jobs in the `ci.yml` workflow.

**Bottom line:** Root causes identified, fixes implemented and verified locally. ~~**NOT pushed to remote**. CI has not re-run on the fixes.~~ DONE: pushed to `origin/fork` (see Resolution below); CI re-ran green.

> **Resolution (2026-07-24):** All 7 commits WERE pushed to `origin/fork`. The lint config fix (`exhaustruct`/`tagliatelle` removed), Windows cross-platform test fixes (`syscall.Dup` → `os.Pipe`), and stats integration tests are all on the remote. The "NOT pushed" claim was true at session end but CI has since re-run on these commits.

---

## What Was Broken

The latest CI run on `21c0685d` (2026-07-21) had **3 failing jobs** out of 6:

| Job                       | Status | Root Cause                                                                                                                              |
| ------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| **Lint**                  | FAIL   | `exhaustruct` + `tagliatelle` linters accidentally enabled by commit `21c0685d` (despite commit message claiming "No semantic changes") |
| **Test (windows-latest)** | FAIL   | `syscall.Dup`/`syscall.Dup2` undefined on Windows + two Unix-only permission tests                                                      |
| **Nix Flake Check**       | FAIL   | Same lint config issue (nix `lint` check runs `golangci-lint run`)                                                                      |

The other 3 jobs (Coverage, Test ubuntu, Test macos) passed.

---

## a) FULLY DONE (verified locally)

### 1. Lint Config Fix

- **File:** `.golangci.yml`
- **Change:** Removed `exhaustruct` and `tagliatelle` from the `linters.enable` list, plus their settings blocks (`exhaustruct.exclude`, `tagliatelle.case.rules`)
- **Root cause:** Commit `21c0685d` ("chore: bump dependencies and reformat golangci config") accidentally added both linters to the enable list. The commit message explicitly claimed "No semantic changes to which linters run" — this was false.
- **AGENTS.md documents both as intentionally DISABLED:** "exhaustruct and tagliatelle are DISABLED globally (exhaustruct: impractical for Go zero-value initialization; tagliatelle: codebase has mixed camelCase/snake_case JSON conventions)"
- **Verified:** `golangci-lint run --timeout 5m ./...` → 0 issues

### 2. Windows Cross-Platform Test Fixes

**2a. syscall.Dup/Dup2 → Cross-Platform Capture Helper**

- **New file:** `internal/testutil/capture.go` — `CaptureStdoutStderr(fn)` and `CaptureCombinedOutput(fn)` using `os.Pipe()` + `os.Stdout`/`os.Stderr` replacement (works on all platforms)
- **Files changed:**
  - `bdd/execute.go` — replaced 30-line fd-duplication block with `testutil.CaptureStdoutStderr(rootCmd.Execute)`
  - `cmd/stats_integration_test.go` — replaced 30-line fd-duplication block with `testutil.CaptureCombinedOutput(rootCmd.Execute)`
- **Verified:** `GOOS=windows GOARCH=amd64 go vet ./...` passes with zero errors

**2b. `TestSaveConfig_DirectoryCreationFails` (config)**

- **Was:** Used `/proc/fake/subdir/config.json` — only fails on Linux (Windows has no `/proc`)
- **Now:** Creates a regular file at `t.TempDir()/not_a_dir`, then tries `MkdirAll` under it — fails on ALL platforms
- **Verified:** `go test ./config/...` passes

**2c. `TestHashFile_UnreadableFile` (hash)**

- **Was:** Set file permission `0o000` and expected `os.Open` to fail — Windows doesn't enforce Unix permissions
- **Now:** `runtime.GOOS == "windows"` skip with clear message
- **Verified:** `go test ./hash/...` passes

### 3. Nix Flake Check

- **Resolved by:** Same lint config fix — the `nix flake check` `lint` check runs `golangci-lint run`
- **Verified:** `nix flake check` → "all checks passed!"

### 4. Full Local Verification Matrix

| Check                 | Command                                    | Result                   |
| --------------------- | ------------------------------------------ | ------------------------ |
| Build                 | `go build ./...`                           | PASS                     |
| Tests                 | `go test ./...`                            | All 26 packages PASS     |
| Race tests            | `go test -race ./...`                      | All 26 packages PASS     |
| Lint                  | `golangci-lint run --timeout 5m ./...`     | 0 issues                 |
| Nix flake             | `nix flake check`                          | all checks passed        |
| Coverage              | `go test -coverprofile=coverage.out ./...` | PASS                     |
| Self-analysis         | `art-dupl stats -t 50 .`                   | PASS (280 files scanned) |
| Windows cross-compile | `GOOS=windows go vet ./...`                | PASS (zero errors)       |
| gofumpt formatting    | `gofumpt -l <changed files>`               | PASS                     |

---

## b) PARTIALLY DONE

### Commits are local but NOT pushed

- 7 commits ahead of `origin/fork`
- CI has NOT re-run on any of the fixes
- The latest CI run visible on GitHub is still the FAILED run from `21c0685d`
- **Push is required for CI to verify the fixes actually pass on GitHub runners**

---

## c) NOT STARTED

- **Did not check other workflows** (`release.yml`, `performance.yml`, `auto-tag.yml`, `deploy-site.yml`, `art-dupl-check.yml`) — they may have similar issues
- **Did not run `art-dupl-check.yml` baseline check** — new code might introduce clones not in `.art-dupl-baseline.json`

---

## d) WHAT GOT FUCKED UP

### 1. Commits have "Unknown Author"

All 7 commits from this session have `Author: Unknown Author <unknown@example.com>` — git user is not configured in this environment. This pollutes git history.

### 2. 7 commits for what should be 1-2

The auto-commit hook split the work into 7 granular commits. This should have been a single focused commit: "fix(ci): remove accidentally-enabled linters and fix Windows test compatibility".

### 3. Generic templated commit messages

Messages like "chore(ci): update golangci-lint configuration for improved code quality" are meaningless templates. They don't explain WHY exhaustruct/tagliatelle were removed, or that the root cause was commit `21c0685d` breaking its own claim of "no semantic changes."

### 4. ~~DID NOT PUSH~~ (DONE — pushed to `origin/fork`, CI green; see Resolution)

The most critical miss. All the local verification in the world is meaningless until CI actually runs green on the remote.

---

## e) WHAT WE SHOULD IMPROVE

1. **CI should run `golangci-lint config verify` or diff the lint config** — commit `21c0685d` claimed "no semantic changes" but added two linters. A config drift check would catch this.
2. **Cross-platform compilation should be a CI step** — `GOOS=windows go vet ./...` takes seconds and would have caught the `syscall.Dup` issue before it reached the test step.
3. **The `syscall.Dup` pattern should never have existed** — Go's standard `os.Stdout` replacement pattern works cross-platform. The fd-duplication approach was unnecessary complexity that only worked on Unix.
4. **Permission-based tests should use `runtime.GOOS` guards** — not assume Linux semantics.
5. **Commit message quality** — the commits generated by hooks in this session are templated filler. Commit messages should explain WHY, reference root causes, and be reviewable by humans.
6. **Consider squashing the 7 commits** into 1 before pushing.

---

## f) Up to 50 Things to Do Next

| #   | Priority     | Task                                                                                                                                |
| --- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **CRITICAL** | Push commits to `origin/fork` so CI runs                                                                                            |
| 2   | **CRITICAL** | Verify CI passes green on GitHub after push                                                                                         |
| 3   | HIGH         | Squash 7 commits into 1 clean commit with proper author + message                                                                   |
| 4   | HIGH         | Fix commit author — set `user.name`/`user.email` in git config                                                                      |
| 5   | HIGH         | Check `performance.yml` workflow for similar failures                                                                               |
| 6   | HIGH         | Check `release.yml` workflow for similar failures                                                                                   |
| 7   | HIGH         | Check `art-dupl-check.yml` — new code may introduce clones outside baseline                                                         |
| 8   | HIGH         | Check `auto-tag.yml` workflow                                                                                                       |
| 9   | HIGH         | Check `deploy-site.yml` workflow                                                                                                    |
| 10  | MEDIUM       | Add `GOOS=windows go vet ./...` as a CI step to catch cross-platform issues early                                                   |
| 11  | MEDIUM       | Add lint config drift detection — diff `.golangci.yml` enable list against expected                                                 |
| 12  | MEDIUM       | Run `art-dupl baseline .` to update `.art-dupl-baseline.json` if new clones exist                                                   |
| 13  | MEDIUM       | Review `CopyToBuffer` — it silently ignores errors from `io.Copy`; if the pipe read fails, the buffer is empty with no signal       |
| 14  | MEDIUM       | Consider adding `CaptureStdoutStderr` unit tests (concurrent capture, large output, error propagation)                              |
| 15  | MEDIUM       | The `captureMu` mutex serializes ALL captures globally — consider if this causes test slowness with many parallel integration tests |
| 16  | LOW          | Update AGENTS.md with the `CaptureStdoutStderr`/`CaptureCombinedOutput` helper as the canonical test output capture method          |
| 17  | LOW          | Audit all `syscall` package imports for Unix-only assumptions                                                                       |
| 18  | LOW          | Consider `//go:build !windows` guard pattern instead of `runtime.GOOS` skip for permission tests                                    |
| 19  | LOW          | Add `golangci-lint config verify` as a pre-commit or CI step                                                                        |
| 20  | LOW          | Review whether the other 100+ linters in `.golangci.yml` are all still desired                                                      |
| 21  | LOW          | Consider adding macOS-specific test guards if any tests assume Linux filesystem behavior                                            |
| 22  | LOW          | Document the CI workflow architecture in AGENTS.md (which jobs depend on which)                                                     |
| 23  | LOW          | Consider adding a CI step that validates `.golangci.yml` matches AGENTS.md documentation                                            |

---

## g) Questions

### 1. Should I push the 7 commits as-is, or squash them first?

The commits have `Unknown Author` and generic messages. Squashing into 1 clean commit with proper attribution would be cleaner, but requires `git reset`/rebase which the AGENTS.md safety rules prohibit (`NEVER git reset`).

### 2. Should I fix the git author configuration for future commits?

The environment has no `user.name`/`user.email` set, resulting in `Unknown Author`. Should I configure this (and to what values), or is this intentional?

### 3. Do you want me to investigate the other 5 workflows now?

I focused only on `ci.yml` as instructed, but `release.yml`, `performance.yml`, `art-dupl-check.yml`, `auto-tag.yml`, and `deploy-site.yml` may have similar latent failures.

---

## Summary

| Category                         | Count               |
| -------------------------------- | ------------------- |
| CI jobs fixed (locally verified) | 3 of 3              |
| Files changed                    | 6                   |
| Lines changed                    | +98 / -95           |
| Local checks passed              | 9 of 9              |
| CI checks passed (remote)        | ~~0 of 6 (NOT PUSHED)~~ 6 of 6 (pushed, CI green) |
| Critical misses                  | ~~1 (did not push)~~ 0 (pushed)    |

---

_Generated 2026-07-22 11:21 by Crush session._
